<UserControl x:Class="CommandPromptFiles.CommandPrompt.Views.CommandPromptControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBox Margin="0,0,0,37" 
                 Text="{Binding OutputBufferText, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="{Binding FontSize, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBox VerticalAlignment="Bottom" Height="32" Margin="0,0,145,0"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="{Binding FontSize, UpdateSourceTrigger=PropertyChanged}">
            <TextBox.InputBindings>
                <KeyBinding Command="{Binding SendCommandCommand}" Key="Enter"/>
            </TextBox.InputBindings>
        </TextBox>
        
        <Button VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="140" Height="32" Content="Send Command"
                Command="{Binding SendCommandCommand}"/>
    </Grid>
</UserControl>
